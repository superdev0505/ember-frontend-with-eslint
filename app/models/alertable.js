/**
* Models inherited from the alertable class can be polymorphically linked to by alert models
*
* @module Alerts
* @class model-alertable
*/
import Model from 'ember-data/model';
// import attr from 'ember-data/attr';
import { } from 'ember-data/relationships';

export default Model.extend({

  // alerts: hasMany('alert', {polymorphic: true, readOnly: true})

});
